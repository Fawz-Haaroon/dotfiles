# Firefox

This repository currently contains only the portable Firefox UI configuration.

## Installation

1. Enable

```
toolkit.legacyUserProfileCustomizations.stylesheets
```

in `about:config`.

2. Copy the `chrome` directory into your Firefox profile.

Example:

```
~/.config/mozilla/firefox/<profile>/chrome
```

Restart Firefox.
